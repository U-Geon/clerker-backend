spring:
  servlet:
    multipart:
      max-file-size: 500MB
      max-request-size: 500MB
  config:
    import:
      - application-db.yml
      - application-jwt.yml
      - application-swagger.yml
      - application-aws.yml
      - application-oauth.yml